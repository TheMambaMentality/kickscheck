<div class="container mt-4">
  <h2><%= current_user.name %> さんのマイページ</h2>

  <div class="my-3">
    <%= link_to "プロフィール編集", edit_user_registration_path, class: "btn btn-outline-primary me-2" %>
    <%= link_to "退会する", registration_path(current_user), method: :delete, data: { confirm: "本当に退会しますか？" }, class: "btn btn-outline-danger" %>
  </div>

  <hr>

  <h4 class="mt-4">あなたの投稿一覧</h4>
  <% if @posts.any? %>
    <div class="row mt-3">
      <% @posts.each do |post| %>
        <div class="col-md-4 mb-4">
          <div class="card h-100 shadow-sm">
            <% if post.images.attached? %>
              <%= image_tag post.images.first.variant(resize_to_limit: [300, 300]), class: "card-img-top" %>
            <% end %>
            <div class="card-body">
              <h5 class="card-title"><%= link_to post.title, post_path(post), class: "text-decoration-none" %></h5>
              <p class="card-text"><%= truncate(post.content, length: 50) %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p>まだ投稿がありません。</p>
  <% end %>
</div>